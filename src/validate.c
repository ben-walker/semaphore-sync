/**
 * Ben Walker
 * CIS*3110
 * 
 * Library to validate the "number of iterations" argument.
 */

#include "validate.h"

/**
 * Returns true if the number of iterations is valid, false otherwise.
 */
bool isValid(int iterations) {
   return iterations > 0;
}
